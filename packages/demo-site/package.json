{
  "name": "@nimbus-goods/demo-site",
  "version": "1.0.0",
  "private": true,
  "description": "Nimbus Goods: a pretend store that embeds the checkout with one script tag.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "vite": "^8.3.1"
  }
}
